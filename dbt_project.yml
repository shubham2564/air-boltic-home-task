name: "air_boltic"
version: "1.0.0"
config-version: 2

profile: "air_boltic"

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  air_boltic:
    staging:
      +materialized: view
    marts:
      +materialized: view
